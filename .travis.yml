sudo: required

services:
  - docker

language: go

before_install:
- curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
- dep ensure

script: go test -cover

# branches:
#   only:
#     - master
